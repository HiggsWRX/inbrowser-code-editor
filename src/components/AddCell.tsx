import { Button } from "./elements";

const AddCell = () => {
  return (
    <div className="py-1 text-center text-2xl">
      <Button onClick={() => {}} className="bg-sky">
        Add Section
      </Button>
    </div>
  );
};

export default AddCell;
